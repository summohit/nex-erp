export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  // Public download link for the Android release build. The APK is ~79MB, so it is
  // hosted externally (Play Store / CDN / object storage) rather than committed to
  // the repo. Leave empty to show a "Coming Soon" state on the landing page.
  androidApkUrl: ''
};
