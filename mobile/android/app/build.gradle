apply plugin: "com.android.application"
apply plugin: "org.jetbrains.kotlin.android"
apply plugin: "com.facebook.react"

/**
 * This is the configuration block to customize your React Native Android app.
 * By default you don't need to apply any configuration, just uncomment the lines you need.
 */
react {
    /* Folders */
    //   The root of your project, i.e. where "package.json" lives. Default is '../..'
    // root = file("../../")
    //   The folder where the react-native NPM package is. Default is ../../node_modules/react-native
    // reactNativeDir = file("../../node_modules/react-native")
    //   The folder where the react-native Codegen package is. Default is ../../node_modules/@react-native/codegen
    // codegenDir = file("../../node_modules/@react-native/codegen")
    //   The cli.js file which is the React Native CLI entrypoint. Default is ../../node_modules/react-native/cli.js
    // cliFile = file("../../node_modules/react-native/cli.js")

    /* Variants */
    //   The list of variants to that are debuggable. For those we're going to
    //   skip the bundling of the JS bundle and the assets. Default is "debug", "debugOptimized".
    //   If you add flavors like lite, prod, etc. you'll have to list your debuggableVariants.
    // debuggableVariants = ["liteDebug", "liteDebugOptimized", "prodDebug", "prodDebugOptimized"]

    /* Bundling */
    //   A list containing the node command and its flags. Default is just 'node'.
    // nodeExecutableAndArgs = ["node"]
    //
    //   The command to run when bundling. By default is 'bundle'
    // bundleCommand = "ram-bundle"
    //
    //   The path to the CLI configuration file. Default is empty.
    // bundleConfig = file(../rn-cli.config.js)
    //
    //   The name of the generated asset file containing your JS bundle
    // bundleAssetName = "MyApplication.android.bundle"
    //
    //   The entry file for bundle generation. Default is 'index.android.js' or 'index.js'
    // entryFile = file("../js/MyApplication.android.js")
    //
    //   A list of extra flags to pass to the 'bundle' commands.
    //   See https://github.com/react-native-community/cli/blob/main/docs/commands.md#bundle
    // extraPackagerArgs = []

    /* Hermes Commands */
    //   The hermes compiler command to run. By default it is 'hermesc'
    // hermesCommand = "$rootDir/my-custom-hermesc/bin/hermesc"
    //
    //   The list of flags to pass to the Hermes compiler. By default is "-O", "-output-source-map"
    // hermesFlags = ["-O", "-output-source-map"]

    /* Autolinking */
    autolinkLibrariesWithApp()
}

/**
 * Set this to true to Run Proguard on Release builds to minify the Java bytecode.
 */
def enableProguardInReleaseBuilds = false

/**
 * The preferred build flavor of JavaScriptCore (JSC)
 *
 * For example, to use the international variant, you can use:
 * `def jscFlavor = io.github.react-native-community:jsc-android-intl:2026004.+`
 *
 * The international variant includes ICU i18n library and necessary data
 * allowing to use e.g. `Date.toLocaleString` and `String.localeCompare` that
 * give correct results when using with locales other than en-US. Note that
 * this variant is about 6MiB larger per architecture than default.
 */
def jscFlavor = 'io.github.react-native-community:jsc-android:2026004.+'

/**
 * Release signing credentials.
 *
 * Never hardcoded here — this file is in git. Credentials come from either
 * android/keystore.properties (gitignored, see keystore.properties.example)
 * or, for CI, the NEX_KEYSTORE_* environment variables.
 *
 * If neither is present the release build FAILS rather than quietly falling
 * back to the debug key. The debug keystore ships with every Android SDK and
 * its password is public ("android"), so a debug-signed "release" build can be
 * impersonated by anyone and is rejected by the Play Store.
 */
def keystorePropertiesFile = rootProject.file("keystore.properties")
def keystoreProperties = new Properties()
if (keystorePropertiesFile.exists()) {
    keystorePropertiesFile.withInputStream { keystoreProperties.load(it) }
}

def releaseStoreFile = keystoreProperties['storeFile'] ?: System.getenv('NEX_KEYSTORE_FILE')
def releaseStorePassword = keystoreProperties['storePassword'] ?: System.getenv('NEX_KEYSTORE_PASSWORD')
def releaseKeyAlias = keystoreProperties['keyAlias'] ?: System.getenv('NEX_KEY_ALIAS')
def releaseKeyPassword = keystoreProperties['keyPassword'] ?: System.getenv('NEX_KEY_PASSWORD')
def hasReleaseKeystore = releaseStoreFile != null && !releaseStoreFile.trim().isEmpty()

android {
    ndkVersion rootProject.ext.ndkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion
    compileSdk rootProject.ext.compileSdkVersion

    namespace "com.nexmobileapp"
    defaultConfig {
        applicationId "com.nexmobileapp"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        // Keep versionName in step with backend/uploads/app/version.json, which
        // drives the version label on the landing page's download button.
        versionCode 241
        versionName "2.4.1"
    }
    signingConfigs {
        debug {
            storeFile file('debug.keystore')
            storePassword 'android'
            keyAlias 'androiddebugkey'
            keyPassword 'android'
        }
        release {
            if (hasReleaseKeystore) {
                // Resolved against the android/ directory (where keystore.properties
                // lives), not app/ — so the property can name the file plainly.
                storeFile rootProject.file(releaseStoreFile)
                storePassword releaseStorePassword
                keyAlias releaseKeyAlias
                keyPassword releaseKeyPassword
            }
        }
    }
    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            signingConfig hasReleaseKeystore ? signingConfigs.release : null
            minifyEnabled enableProguardInReleaseBuilds
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
    }
}

// Fail the release build early and loudly when no signing key is configured,
// instead of emitting an unsigned APK that fails at install time with an
// error that points nowhere near the actual cause.
tasks.configureEach { task ->
    if (task.name == "packageRelease" && !hasReleaseKeystore) {
        task.doFirst {
            throw new GradleException(
                "No release keystore configured.\n" +
                "Create android/keystore.properties from keystore.properties.example\n" +
                "(or set NEX_KEYSTORE_FILE / NEX_KEYSTORE_PASSWORD / NEX_KEY_ALIAS / NEX_KEY_PASSWORD).\n" +
                "See keystore.properties.example for the keytool command."
            )
        }
    }
}

// Gradle resolves version conflicts by picking the highest, so an ext var alone
// is not enough — any transitive dep asking for 21.x would win and reintroduce
// the FusedLocationProviderClient IncompatibleClassChangeError crash.
configurations.all {
    resolutionStrategy {
        force "com.google.android.gms:play-services-location:20.0.0"
    }
}

dependencies {
    // The version of react-native is set by the React Native Gradle Plugin
    implementation("com.facebook.react:react-android")

    if (hermesEnabled.toBoolean()) {
        implementation("com.facebook.react:hermes-android")
    } else {
        implementation jscFlavor
    }

    // First-party Kotlin (FieldVisitLocationService) calls FusedLocationProviderClient
    // directly, so declare it explicitly rather than leaning on react-native-maps'
    // transitive dependency — pinned to 20.0.0 for the same reason as the force{} above.
    implementation "com.google.android.gms:play-services-location:${rootProject.ext.googlePlayServicesVersion}"
}
